#include<iostream> 
#include<list> 
using namespace std;

int main()
{
     list<int> fila;
     int x;
     
     //inserir elementos na lista
     for(int i = 0; i < 4; i++)
	 {
	 	cin >> x;
	 	fila.push_back(x);
	 }
	
	//mostrar elementos na ordem em que foram inseridos
	 while(!fila.empty())
	 {
	 	cout<< *fila.begin() << endl;
	 	fila.pop_front();
	 }
	
	return 0;
}
